import { FormColumnsType } from '@/dynamic-components';

export const transfer: FormColumnsType = {
  title: '',
  dataIndex: 'transfer',
  valueType: 'transfer',
  editable: false,
  hideInSearch: true,
};
